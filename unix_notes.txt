cd
history | sort | less
history | sort
less --help
h
less
/subl
history | less
history
curl -fsSL http://git.io/QxUaOQ | sh
curl -fsSL http://git.io/7UHtNA | sh
git config --global user.email 'jeremyrschneider@gmail.com'
git config --global user.name 'jeremyrs'
git --version
brew install git
echo $PATH
echo $P
/ect/paths
echo $EDITOR
subl ~/.config/fish/config.fish
ln -s /Applications/Sublime\ Text\ 2.app/Contents/SharedSupport/bin/subl /usr/local/bin/subl
ls
cd /Applications/
open /Applications/
subl
brew info fish
subl .
cd Applications
cd //
fish_update_completions
open /usr/local/bin/
open ~/.config/fish/config.fish
brew update
cd /usr/local/bin/subl
open subl
cd /usr/local/bin/
subl
brew install fish
fish_update_completions
curl -fsSL http://git.io/beJs | ruby
chsh -s /usr/local/bin/fish
echo '/usr/local/bin/fish' |sudo tee -a /etc/shells
echo '/usr/local/bin/fish' | sudo tee -a /ect/shells
echo '/usr/local/binfish' | sudo tee -a /ect/shells
brew doctor
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
ls -G
uname
>> output is added to existing file.
> will orver right pervious file
| is the pipe character it takes the output for the 1st command and uses that as input for the 2nd command
.. in comand brings up one directory
../.. brings you up two directories
less --help  (summary of commands)
cd (only brings you to your root directory)
~ (tilda represents your root directory)
ln (creates a link)

subl . will pull up all files in current directory
